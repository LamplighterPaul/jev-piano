# Secret *references*, not secrets: `scripts/with-key.sh` resolves these with
# `op run` so the key is never written to disk. Copy to .env.op and edit.
# Plain `TYPESAFE_API_KEY=...` in the environment works just as well.
TYPESAFE_API_KEY=op://<vault-id>/<item-id>/credential
